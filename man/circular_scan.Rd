% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/circular_scan.R
\name{circular_scan}
\alias{circular_scan}
\title{Kulldorff's Circular Spatial Scan Statistic}
\usage{
circular_scan(
  cases,
  population,
  region_id,
  x,
  y,
  max_pop_pct = 0.5,
  nsim = 999L,
  alpha = 0.05,
  n_secondary = 1000L,
  model = c("poisson", "binomial"),
  seed = NULL,
  n_cores = 1L
)
}
\arguments{
\item{cases}{Numeric vector of length \eqn{n} (one entry per region):
total cases in each region.}

\item{population}{Numeric vector of length \eqn{n}: population (or
denominator) of each region.}

\item{region_id}{Vector of region identifiers, length \eqn{n}.}

\item{x, y}{Numeric vectors of region centroid coordinates, length \eqn{n}.}

\item{max_pop_pct}{Numeric. Default \code{0.5}.}

\item{nsim}{Integer. Number of MC simulations. Default \code{999}.}

\item{alpha}{Numeric. Significance level. Default \code{0.05}.}

\item{n_secondary}{Integer. Default \code{1000}.}

\item{model}{Character. \code{"poisson"} or \code{"binomial"}.}

\item{seed}{Integer or \code{NULL}.}

\item{n_cores}{Integer. OpenMP threads.}
}
\value{
An object of class \code{"circular_scan"}.
}
\description{
Performs Kulldorff's circular spatial scan statistic for detecting
spatial clusters. Inputs are passed as parallel vectors with one entry
per region (cases must already be aggregated to the region level).
}
\examples{
set.seed(42)
n <- 20
cases <- rpois(n, lambda = 10)
cases[1:5] <- rpois(5, lambda = 30)

result <- circular_scan(
  cases       = cases,
  population  = rep(1000, n),
  region_id   = 1:n,
  x           = runif(n, 0, 10),
  y           = runif(n, 0, 10),
  nsim        = 99
)
print(result)
}
\references{
Kulldorff, M. (1997). A spatial scan statistic. \emph{Communications in
Statistics - Theory and Methods}, 26(6), 1481-1496.
}
\seealso{
\code{\link{filter_clusters}}, \code{\link{tree_scan}},
  \code{\link{treespatial_scan}}, \code{\link{get_cluster_regions}},
  \code{\link{iterative_scan}}
}
